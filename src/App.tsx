/**
 * Placeholder shell — owned by the Frontend Developer.
 * Replace with the real Start / Play / Game Over screen router wired to
 * `useGame`. Kept minimal so the scaffold boots and the smoke test passes.
 */
export default function App() {
  return (
    <main className="flex min-h-full items-center justify-center">
      <h1 className="text-2xl font-bold">VimRace</h1>
    </main>
  );
}
